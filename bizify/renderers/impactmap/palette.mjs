// Business kind -> viewer palette slot (frontend|backend|database|cloud|security|messagebus|external).
// One family per Adzic level so the four columns read apart at a glance.
export const IMPACTMAP_KIND_PALETTE = {
  'impact-goal': 'cloud',
  'impact-actor': 'frontend',
  'impact-change': 'database',
  'impact-deliverable': 'backend',
};
