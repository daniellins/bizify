// Business kind -> viewer palette slot. The slots are the seven semantic
// color families every viewer preset and theme already defines, so a kind
// keeps its color in the overview map, Semantic Lens, and exports.
export const WBS_KIND_PALETTE = {
  project: 'external',
  phase: 'database',
  deliverable: 'frontend',
  'work-package': 'backend',
  'planning-package': 'cloud',
};
